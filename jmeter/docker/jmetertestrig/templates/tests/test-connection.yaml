apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "jmetertestrig.fullname" . }}-test-connection"
  labels:
    {{- include "jmetertestrig.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "jmetertestrig.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
