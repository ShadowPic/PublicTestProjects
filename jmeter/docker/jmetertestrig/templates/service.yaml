apiVersion: v1
kind: Service
metadata:
  name: {{ include "jmetertestrig.fullname" . }}
  labels:
    {{- include "jmetertestrig.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jmetertestrig.selectorLabels" . | nindent 4 }}
